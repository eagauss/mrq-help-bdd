## What changed

<!-- One or two sentences. The "why" matters more than the "what". -->

## How it was tested

- [ ] `npm test` passes locally
- [ ] Ran the workflow manually from the Actions tab and skimmed the CTRF report on the Actions run
- [ ] (If feature/step changes) opened the HTML report and checked the affected scenarios

## Notes for the reviewer

<!-- Anything worth flagging — known flake, breaking selector change, follow-up needed. Leave blank if nothing. -->
